---
phase: 01-backend-foundation-data-import
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/main.py
  - backend/app/api/upload.py
  - backend/app/models/upload.py
  - backend/app/core/config.py
  - backend/tests/test_upload.py
  - docker-compose.yml
  - backend/requirements.txt
autonomous: true
requirements: [IMPORT-01, IMPORT-03]

must_haves:
  truths:
    - "Benutzer kann CSV-Dateien über Web-Interface hochladen"
    - "Hochgeladene Dateien werden in zeitstempelbasierten Ordnern gespeichert"
    - "System zeigt Bestätigung mit Dateinamen nach erfolgreichem Upload"
    - "Benutzer kann Bilder-Ordner hochladen"
  artifacts:
    - path: "backend/app/api/upload.py"
      provides: "Upload-Endpunkte für CSV und Bilder"
      exports: ["upload_csv", "upload_images"]
    - path: "backend/app/models/upload.py"
      provides: "Datenmodelle für Upload-Session"
      contains: "class UploadSession"
    - path: "uploads/"
      provides: "File-Storage mit Zeitstempel-Struktur"
      pattern: "uploads/YYYY-MM-DD_HHMMSS/"
  key_links:
    - from: "frontend"
      to: "backend/app/api/upload.py"
      via: "POST /api/upload/csv"
      pattern: "multipart/form-data"
    - from: "backend/app/api/upload.py"
      to: "uploads/"
      via: "file storage"
      pattern: "Path.*uploads"
---

# Plan 01: Backend Upload API + File Storage Infrastructure

<objective>
Erstelle FastAPI Backend mit Upload-Endpoints für CSV-Dateien und Bilder-Ordner. Implementiere zeitstempelbasierte File-Storage-Struktur für Upload-Sessions.

**Purpose:** Foundation für alle weiteren Phasen - ohne funktionierende Uploads können keine CSVs analysiert oder Kataloge generiert werden.

**Output:** Funktionierendes Backend das CSV- und Bild-Uploads entgegennimmt, in strukturierten Ordnern speichert und Upload-Metadaten zurückgibt.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-backend-foundation-data-import/01-CONTEXT.md
@.planning/phases/01-backend-foundation-data-import/01-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: FastAPI Projekt-Struktur + Docker Setup</name>
  <files>backend/app/main.py, backend/app/core/config.py, backend/requirements.txt, docker-compose.yml, backend/Dockerfile, .env.example</files>
  
  <read_first>
- copilot-instructions.md (Projekt-spezifische Richtlinien)
- .planning/phases/01-backend-foundation-data-import/01-RESEARCH.md (Stack: FastAPI 0.110+, Python 3.11+, Uvicorn)
  </read_first>
  
  <action>
Erstelle FastAPI Projekt mit folgender Struktur:

```
backend/
├── app/
│   ├── __init__.py
│   ├── main.py          # FastAPI app instance, CORS, Routers
│   ├── core/
│   │   ├── __init__.py
│   │   └── config.py    # Settings mit pydantic-settings
│   └── api/
│       ├── __init__.py
│       └── upload.py    # Upload routes (erstellt in Task 2)
├── requirements.txt
├── Dockerfile
└── tests/
    └── __init__.py
```

**main.py:**
```python
from fastapi import FastAPI
from fastapi.middleware.cors import CORSMiddleware
from app.core.config import settings
from app.api import upload

app = FastAPI(
    title="Katalog API",
    description="Backend für Produktkatalog-Generierung",
    version="1.0.0"
)

# CORS für Frontend
app.add_middleware(
    CORSMiddleware,
    allow_origins=["http://localhost:3000", "http://localhost:5173"],  # React dev servers
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)

# Routers
app.include_router(upload.router, prefix="/api", tags=["upload"])

@app.get("/health")
async def health_check():
    return {"status": "healthy", "service": "katalog-api"}
```

**core/config.py:**
```python
from pydantic_settings import BaseSettings
from pathlib import Path

class Settings(BaseSettings):
    # App
    app_name: str = "Katalog API"
    debug: bool = True
    
    # Upload
    upload_dir: Path = Path("uploads")
    max_csv_size_mb: int = 50
    max_image_size_mb: int = 10
    allowed_csv_extensions: set[str] = {".csv"}
    allowed_image_extensions: set[str] = {".jpg", ".jpeg", ".png", ".gif"}
    
    class Config:
        env_file = ".env"

settings = Settings()
```

**requirements.txt:**
```
fastapi==0.110.0
uvicorn[standard]==0.27.1
pydantic==2.6.1
pydantic-settings==2.1.0
python-multipart==0.0.9  # Für file uploads
aiofiles==23.2.1  # Async file operations
pytest==8.0.0
pytest-asyncio==0.23.5
httpx==0.27.0  # Für tests
```

**docker-compose.yml:**
```yaml
version: '3.8'

services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/app
      - ./uploads:/app/uploads
    environment:
      - DEBUG=true
      - UPLOAD_DIR=/app/uploads
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload

  # Frontend wird in Plan 03 hinzugefügt
```

**backend/Dockerfile:**
```dockerfile
FROM python:3.11-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . .

CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000"]
```

**.env.example:**
```
DEBUG=true
UPLOAD_DIR=./uploads
MAX_CSV_SIZE_MB=50
MAX_IMAGE_SIZE_MB=10
```
  </action>
  
  <acceptance_criteria>
- `backend/app/main.py` exists and contains `app = FastAPI(`
- `backend/app/core/config.py` contains `class Settings(BaseSettings)` with `upload_dir` field
- `backend/requirements.txt` lists fastapi==0.110.0, uvicorn, pydantic==2.6.1, python-multipart, aiofiles
- `docker-compose.yml` contains service named `backend` with port 8000 exposed
- `backend/Dockerfile` contains `FROM python:3.11-slim` and uvicorn CMD
- `.env.example` contains UPLOAD_DIR and MAX_CSV_SIZE_MB variables
  </acceptance_criteria>
  
  <verify>
```bash
# Backend startet ohne Fehler
cd backend && python -m app.main 2>&1 | grep -q "Application startup complete"

# Health endpoint erreichbar
docker-compose up -d backend && sleep 3
curl http://localhost:8000/health | grep -q '"status":"healthy"'
docker-compose down
```
  </verify>
  
  <done>
- FastAPI app läuft und health-check antwortet mit 200
- Docker container startet ohne Fehler
- Settings laden UPLOAD_DIR korrekt aus .env
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Upload API Endpoints (CSV + Images)</name>
  <files>backend/app/api/upload.py, backend/app/models/upload.py</files>
  
  <read_first>
- backend/app/main.py (fastapi app instance um router zu registrieren)
- backend/app/core/config.py (settings für upload_dir, file size limits)
- .planning/phases/01-backend-foundation-data-import/01-CONTEXT.md (D-03: Zeitstempel-Ordner, D-06: Getrennte Schritte)
- .planning/phases/01-backend-foundation-data-import/01-RESEARCH.md (Upload implementation patterns)
  </read_first>
  
  <action>
**backend/app/models/upload.py:**
```python
from pydantic import BaseModel, Field
from datetime import datetime
from pathlib import Path

class UploadSession(BaseModel):
    """Upload-Session Metadaten"""
    upload_id: str
    created_at: datetime = Field(default_factory=datetime.now)
    upload_dir: Path
    csv_files: list[str] = []
    image_count: int = 0
    
class CSVUploadResponse(BaseModel):
    upload_id: str
    filename: str
    size_bytes: int
    uploaded_at: datetime
    path: str  # Relative path from uploads/
    
class ImageUploadResponse(BaseModel):
    upload_id: str
    image_count: int
    total_size_bytes: int
    uploaded_at: datetime
    image_dir: str
```

**backend/app/api/upload.py:**
```python
from fastapi import APIRouter, UploadFile, File, HTTPException, BackgroundTasks
from fastapi.responses import JSONResponse
from pathlib import Path
from datetime import datetime
import aiofiles
import os

from app.core.config import settings
from app.models.upload import CSVUploadResponse, ImageUploadResponse, UploadSession

router = APIRouter()

def create_upload_session() -> tuple[str, Path]:
    """Erstellt Zeitstempel-Ordner für Upload-Session (CONTEXT D-03)"""
    upload_id = datetime.now().strftime("%Y-%m-%d_%H%M%S")
    upload_dir = settings.upload_dir / upload_id
    upload_dir.mkdir(parents=True, exist_ok=True)
    return upload_id, upload_dir

@router.post("/upload/csv", response_model=CSVUploadResponse)
async def upload_csv(
    file: UploadFile = File(...),
):
    """
    Upload CSV file
    
    CONTEXT D-06: Getrennte Upload-Schritte (CSVs zuerst)
    RESEARCH: Streaming upload für große Dateien
    """
    # Validierung: CSV extension
    if not file.filename.lower().endswith('.csv'):
        raise HTTPException(
            status_code=400,
            detail=f"Invalid file type. Expected .csv, got {Path(file.filename).suffix}"
        )
    
    # Validierung: File size (Basic check, real size validated during upload)
    if file.size and file.size > settings.max_csv_size_mb * 1024 * 1024:
        raise HTTPException(
            status_code=413,
            detail=f"File too large. Max {settings.max_csv_size_mb}MB allowed."
        )
    
    # Create upload session with timestamp folder
    upload_id, upload_dir = create_upload_session()
    
    # Save file with streaming (memory-efficient for large files)
    csv_path = upload_dir / file.filename
    file_size = 0
    
    async with aiofiles.open(csv_path, 'wb') as f:
        while chunk := await file.read(8192):  # 8KB chunks (RESEARCH pattern)
            file_size += len(chunk)
            await f.write(chunk)
    
    return CSVUploadResponse(
        upload_id=upload_id,
        filename=file.filename,
        size_bytes=file_size,
        uploaded_at=datetime.now(),
        path=str(csv_path.relative_to(settings.upload_dir))
    )

@router.post("/upload/images/{upload_id}", response_model=ImageUploadResponse)
async def upload_images(
    upload_id: str,
    files: list[UploadFile] = File(...),
):
    """
    Upload multiple image files for existing upload session
    
    CONTEXT D-06: Getrennte Upload-Schritte (Bilder nach CSVs)
    """
    upload_dir = settings.upload_dir / upload_id
    
    # Validate upload session exists
    if not upload_dir.exists():
        raise HTTPException(
            status_code=404,
            detail=f"Upload session {upload_id} not found. Upload CSV first."
        )
    
    # Create bilder subdirectory (matches PROJECT.md structure)
    images_dir = upload_dir / "bilder"
    images_dir.mkdir(exist_ok=True)
    
    total_size = 0
    uploaded_count = 0
    
    for image in files:
        # Validate image extension
        ext = Path(image.filename).suffix.lower()
        if ext not in settings.allowed_image_extensions:
            # Skip invalid files, don't block entire upload
            continue
        
        # Save image
        image_path = images_dir / image.filename
        file_size = 0
        
        async with aiofiles.open(image_path, 'wb') as f:
            while chunk := await image.read(16384):  # 16KB chunks for images
                file_size += len(chunk)
                await f.write(chunk)
        
        total_size += file_size
        uploaded_count += 1
    
    return ImageUploadResponse(
        upload_id=upload_id,
        image_count=uploaded_count,
        total_size_bytes=total_size,
        uploaded_at=datetime.now(),
        image_dir=str(images_dir.relative_to(settings.upload_dir))
    )

@router.get("/upload/{upload_id}")
async def get_upload_session(upload_id: str):
    """Get upload session info"""
    upload_dir = settings.upload_dir / upload_id
    
    if not upload_dir.exists():
        raise HTTPException(status_code=404, detail="Upload session not found")
    
    # Count CSVs and images
    csv_files = list(upload_dir.glob("*.csv"))
    images_dir = upload_dir / "bilder"
    image_files = []
    if images_dir.exists():
        for ext in settings.allowed_image_extensions:
            image_files.extend(images_dir.glob(f"*{ext}"))
    
    return {
        "upload_id": upload_id,
        "csv_count": len(csv_files),
        "csv_files": [f.name for f in csv_files],
        "image_count": len(image_files),
        "upload_dir": str(upload_dir)
    }
```

Registriere Router in `main.py` (bereits in Task 1 vorbereitet):
```python
app.include_router(upload.router, prefix="/api", tags=["upload"])
```
  </action>
  
  <acceptance_criteria>
- `backend/app/models/upload.py` contains `class UploadSession(BaseModel)` with upload_id field
- `backend/app/models/upload.py` contains `class CSVUploadResponse` and `class ImageUploadResponse`
- `backend/app/api/upload.py` contains `@router.post("/upload/csv")` endpoint
- `backend/app/api/upload.py` contains `@router.post("/upload/images/{upload_id}")` endpoint
- `backend/app/api/upload.py` contains `create_upload_session()` function that creates timestamp folders matching pattern `YYYY-MM-DD_HHMMSS`
- Upload endpoints use `aiofiles` for async file streaming (no `with open()`, only `async with aiofiles.open()`)
- CSV upload creates `uploads/{timestamp}/` directory
- Image upload creates `uploads/{timestamp}/bilder/` subdirectory
  </acceptance_criteria>
  
  <verify>
```bash
# Test CSV upload
docker-compose up -d backend && sleep 3
RESPONSE=$(curl -X POST -F "file=@assets/EDI Export Artikeldaten.csv" http://localhost:8000/api/upload/csv)
echo "$RESPONSE" | grep -q '"upload_id":'
UPLOAD_ID=$(echo "$RESPONSE" | python3 -c "import sys, json; print(json.load(sys.stdin)['upload_id'])")

# Verify timestamp folder created
test -d "uploads/${UPLOAD_ID}"

# Test image upload
curl -X POST -F "files=@assets/bilder/test.jpg" "http://localhost:8000/api/upload/images/${UPLOAD_ID}" | grep -q '"image_count":'

# Verify bilder subfolder
test -d "uploads/${UPLOAD_ID}/bilder"

docker-compose down
```
  </verify>
  
  <done>
- POST /api/upload/csv akzeptiert CSV-Datei und gibt upload_id zurück
- Upload erstellt Zeitstempel-Ordner format YYYY-MM-DD_HHMMSS
- POST /api/upload/images/{upload_id} akzeptiert multiple Bilder
- Bilder werden in {upload_id}/bilder/ Unterordner gespeichert
- GET /api/upload/{upload_id} zeigt CSV- und Bild-Counts
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Integration Tests für Upload Flow</name>
  <files>backend/tests/test_upload.py, backend/tests/conftest.py</files>
  
  <read_first>
- backend/app/api/upload.py (endpoints to test)
- backend/app/models/upload.py (response models)
- backend/app/core/config.py (settings für test environment)
  </read_first>
  
  <action>
**backend/tests/conftest.py:**
```python
import pytest
from fastapi.testclient import TestClient
from pathlib import Path
import shutil

from app.main import app
from app.core.config import settings

@pytest.fixture
def client():
    """Test client für FastAPI"""
    return TestClient(app)

@pytest.fixture
def test_upload_dir(tmp_path):
    """Temporary upload directory für Tests"""
    upload_dir = tmp_path / "test_uploads"
    upload_dir.mkdir()
    
    # Override settings
    settings.upload_dir = upload_dir
    
    yield upload_dir
    
    # Cleanup
    if upload_dir.exists():
        shutil.rmtree(upload_dir)

@pytest.fixture
def sample_csv(tmp_path):
    """Create sample CSV file"""
    csv_file = tmp_path / "test_data.csv"
    csv_file.write_text(
        "Artikelnummer,Bezeichnung1,Preis\n"
        "D80950,Müllbehälter 120L,45.99\n"
        "D80951,Abfalltonne 240L,89.99\n",
        encoding="utf-8"
    )
    return csv_file

@pytest.fixture
def sample_image(tmp_path):
    """Create minimal image file"""
    image_file = tmp_path / "test_image.jpg"
    # Minimal JPEG header
    image_file.write_bytes(b'\xFF\xD8\xFF\xE0' + b'\x00' * 100)
    return image_file
```

**backend/tests/test_upload.py:**
```python
import pytest
from pathlib import Path
from datetime import datetime

def test_csv_upload_creates_timestamp_folder(client, sample_csv, test_upload_dir):
    """Test CONTEXT D-03: Zeitstempel-Ordner pro Upload"""
    with open(sample_csv, 'rb') as f:
        response = client.post(
            "/api/upload/csv",
            files={"file": ("test.csv", f, "text/csv")}
        )
    
    assert response.status_code == 200
    data = response.json()
    assert "upload_id" in data
    assert "filename" in data
    assert data["filename"] == "test.csv"
    
    # Verify timestamp format YYYY-MM-DD_HHMMSS
    upload_id = data["upload_id"]
    assert len(upload_id) == 19  # 2026-03-25_143022
    assert upload_id[4] == "-" and upload_id[7] == "-" and upload_id[10] == "_"
    
    # Verify folder exists
    upload_folder = test_upload_dir / upload_id
    assert upload_folder.exists()
    assert (upload_folder / "test.csv").exists()

def test_csv_upload_rejects_non_csv_files(client, tmp_path):
    """Test file type validation"""
    txt_file = tmp_path / "test.txt"
    txt_file.write_text("not a csv")
    
    with open(txt_file, 'rb') as f:
        response = client.post(
            "/api/upload/csv",
            files={"file": ("test.txt", f, "text/plain")}
        )
    
    assert response.status_code == 400
    assert "Invalid file type" in response.json()["detail"]

def test_separate_upload_steps(client, sample_csv, sample_image, test_upload_dir):
    """Test CONTEXT D-06: Getrennte Schritte (erst CSVs, dann Bilder)"""
    # Step 1: Upload CSV
    with open(sample_csv, 'rb') as f:
        csv_response = client.post(
            "/api/upload/csv",
            files={"file": ("test.csv", f, "text/csv")}
        )
    assert csv_response.status_code == 200
    upload_id = csv_response.json()["upload_id"]
    
    # Step 2: Upload images to same session
    with open(sample_image, 'rb') as f:
        img_response = client.post(
            f"/api/upload/images/{upload_id}",
            files={"files": ("test.jpg", f, "image/jpeg")}
        )
    assert img_response.status_code == 200
    data = img_response.json()
    assert data["image_count"] == 1
    assert data["upload_id"] == upload_id
    
    # Verify bilder subfolder created
    bilder_dir = test_upload_dir / upload_id / "bilder"
    assert bilder_dir.exists()
    assert (bilder_dir / "test.jpg").exists()

def test_image_upload_requires_existing_session(client, sample_image):
    """Test image upload validates CSV was uploaded first"""
    with open(sample_image, 'rb') as f:
        response = client.post(
            "/api/upload/images/nonexistent-session",
            files={"files": ("test.jpg", f, "image/jpeg")}
        )
    
    assert response.status_code == 404
    assert "not found" in response.json()["detail"].lower()

def test_get_upload_session_info(client, sample_csv, sample_image, test_upload_dir):
    """Test upload session retrieval"""
    # Upload CSV
    with open(sample_csv, 'rb') as f:
        csv_response = client.post(
            "/api/upload/csv",
            files={"file": ("test.csv", f, "text/csv")}
        )
    upload_id = csv_response.json()["upload_id"]
    
    # Upload image
    with open(sample_image, 'rb') as f:
        client.post(
            f"/api/upload/images/{upload_id}",
            files={"files": ("test.jpg", f, "image/jpeg")}
        )
    
    # Get session info
    response = client.get(f"/api/upload/{upload_id}")
    assert response.status_code == 200
    data = response.json()
    assert data["csv_count"] == 1
    assert data["image_count"] == 1
    assert "test.csv" in data["csv_files"]

def test_multiple_csv_uploads_create_separate_sessions(client, sample_csv, test_upload_dir):
    """Test each CSV upload gets own timestamp folder"""
    # Upload 1
    with open(sample_csv, 'rb') as f:
        response1 = client.post(
            "/api/upload/csv",
            files={"file": ("test1.csv", f, "text/csv")}
        )
    upload_id1 = response1.json()["upload_id"]
    
    # Upload 2 (minimal delay to ensure different timestamp)
    import time
    time.sleep(1)
    
    with open(sample_csv, 'rb') as f:
        response2 = client.post(
            "/api/upload/csv",
            files={"file": ("test2.csv", f, "text/csv")}
        )
    upload_id2 = response2.json()["upload_id"]
    
    # Verify separate folders
    assert upload_id1 != upload_id2
    assert (test_upload_dir / upload_id1).exists()
    assert (test_upload_dir / upload_id2).exists()
```
  </action>
  
  <acceptance_criteria>
- `backend/tests/conftest.py` contains `@pytest.fixture` for `client`, `test_upload_dir`, `sample_csv`, `sample_image`
- `backend/tests/test_upload.py` contains test for timestamp folder creation (tests CONTEXT D-03)
- `backend/tests/test_upload.py` contains test for separate upload steps (tests CONTEXT D-06)
- `backend/tests/test_upload.py` contains test for file type validation (rejects non-CSV)
- `backend/tests/test_upload.py` contains test that verifies `bilder/` subfolder is created
- All tests use `TestClient` from fastapi.testclient
- Tests verify actual file creation on disk, not just HTTP responses
  </acceptance_criteria>
  
  <verify>
```bash
cd backend
pytest tests/test_upload.py -v
# All tests should pass
```
  </verify>
  
  <done>
- pytest läuft ohne Fehler
- Tests decken: Zeitstempel-Ordner-Erstellung, File-Type-Validierung, getrennte Schritte (CSV → Bilder), Session-Info-Abruf
- Temporäre Test-Uploads werden nach Tests aufgeräumt
  </done>
</task>

</tasks>

<verification>
**Gesamt-Phase-Checks:**

1. **Upload-Flow funktioniert end-to-end:**
```bash
# Start backend
docker-compose up -d backend

# Upload CSV
UPLOAD=$(curl -X POST -F "file=@assets/EDI Export Artikeldaten.csv" http://localhost:8000/api/upload/csv)
UPLOAD_ID=$(echo $UPLOAD | jq -r '.upload_id')

# Verify folder structure
test -f "uploads/${UPLOAD_ID}/EDI Export Artikeldaten.csv" || echo "CSV not found"

# Upload images
curl -X POST -F "files=@assets/bilder/test1.jpg" -F "files=@assets/bilder/test2.jpg" \
  "http://localhost:8000/api/upload/images/${UPLOAD_ID}"

# Verify bilder folder
test -d "uploads/${UPLOAD_ID}/bilder" || echo "Bilder folder not found"

# Get session info
curl "http://localhost:8000/api/upload/${UPLOAD_ID}" | jq '.'

docker-compose down
```

2. **Zeitstempel-Format korrekt:**
```bash
ls uploads/ | grep -E '^[0-9]{4}-[0-9]{2}-[0-9]{2}_[0-9]{6}$' || echo "Invalid timestamp format"
```

3. **Tests bestehen:**
```bash
cd backend && pytest tests/test_upload.py --tb=short
```
</verification>

<success_criteria>
1. Benutzer kann CSV-Dateien über POST /api/upload/csv hochladen und erhält upload_id zurück (REQ IMPORT-01 partial)
2. Hochgeladene Dateien werden in Zeitstempel-Ordnern gespeichert (Format YYYY-MM-DD_HHMMSS) gemäß CONTEXT D-03
3. Benutzer kann Bilder-Ordner über POST /api/upload/images/{upload_id} hochladen (REQ IMPORT-03 partial)
4. System erstellt `bilder/` Unterordner automatisch (entspricht PROJECT.md Struktur)
5. Backend startet via Docker Compose ohne Fehler
6. Alle Integration-Tests bestehen
</success_criteria>

<output>
Nach Fertigstellung erstelle `.planning/phases/01-backend-foundation-data-import/01-01-SUMMARY.md` mit:
- Implementierte Endpoints und deren Signaturen
- Dateistruktur-Beispiele
- Test-Coverage
- Offene Punkte für Plan 02 (Encoding-Detection, Validation)
</output>
