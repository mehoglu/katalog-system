---
phase: 01-backend-foundation-data-import
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - backend/app/services/encoding.py
  - backend/app/services/validation.py
  - backend/app/models/validation.py
  - backend/app/api/upload.py
  - backend/requirements.txt
  - backend/tests/test_encoding.py
  - backend/tests/test_validation.py
autonomous: true
requirements: [IMPORT-02, IMPORT-04]

must_haves:
  truths:
    - "System erkennt CSV-Encoding automatisch (Windows-1252 vs UTF-8)"
    - "Benutzer sieht Bestätigung des erkannten Encodings"
    - "Deutsche Umlaute (ä,ö,ü,ß) werden korrekt angezeigt"
    - "System validiert CSV-Struktur und zeigt Warnungen"
    - "Validierungs-Warnungen blockieren Workflow nicht (non-blocking)"
  artifacts:
    - path: "backend/app/services/encoding.py"
      provides: "Encoding detection mit charset-normalizer"
      exports: ["detect_encoding", "convert_to_utf8"]
    - path: "backend/app/services/validation.py"
      provides: "CSV structure validation"
      exports: ["validate_csv_structure"]
    - path: "backend/app/models/validation.py"
      provides: "ValidationError und ValidationResult models"
      contains: "class ValidationError"
  key_links:
    - from: "backend/app/api/upload.py"
      to: "backend/app/services/encoding.py"
      via: "detect_encoding(csv_path)"
      pattern: "encoding\\.detect_encoding"
    - from: "backend/app/services/validation.py"
      to: "polars"
      via: "CSV parsing"
      pattern: "pl\\.scan_csv"
---

# Plan 02: Encoding Detection + CSV Validation

<objective>
Implementiere automatische Encoding-Erkennung für deutsche CSVs (Windows-1252/UTF-8) und strukturelle CSV-Validierung mit nicht-blockierenden Warnungen.

**Purpose:** Löst **Critical Pitfall #3** aus Research - verhindert Umlaut-Korruption ("Müll" → "MÃ¼ll"). Validierung zeigt Probleme früh, aber blockiert Workflow nicht (User kann später in Phase 6 korrigieren).

**Output:** CSVs werden automatisch in UTF-8 konvertiert mit korrekten Umlauten. Validierung gibt strukturierte Fehler/Warnungen zurück die frontend (Plan 03) anzeigen kann.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@ .github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-backend-foundation-data-import/01-CONTEXT.md
@.planning/phases/01-backend-foundation-data-import/01-RESEARCH.md
@.planning/phases/01-backend-foundation-data-import/01-01-PLAN.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Encoding Detection Service</name>
  <files>backend/app/services/encoding.py, backend/requirements.txt</files>
  
  <read_first>
- .planning/phases/01-backend-foundation-data-import/01-RESEARCH.md (charset-normalizer usage, Windows-1252 fallback)
- .planning/phases/01-backend-foundation-data-import/01-CONTEXT.md (D-17 bis D-20: Server-side, charset-normalizer, user confirmation, Windows-1252 fallback)
  </read_first>
  
  <action>
Installiere charset-normalizer:

**backend/requirements.txt** (append):
```
charset-normalizer==3.3.2
polars==0.20.10
```

**backend/app/services/__init__.py:**
```python
# Services module
```

**backend/app/services/encoding.py:**
```python
"""
Encoding detection and conversion service
Addresses Critical Pitfall #3: German encoding corruption
"""
from pathlib import Path
from charset_normalizer import from_bytes
from dataclasses import dataclass
from typing import Optional

@dataclass
class EncodingResult:
    """Ergebnis der Encoding-Erkennung"""
    detected_encoding: str
    confidence: float
    is_fallback: bool  # True wenn Fallback zu Windows-1252
    needs_confirmation: bool  # True wenn confidence < 70%

def detect_encoding(file_path: Path) -> EncodingResult:
    """
    Erkennt CSV-Encoding automatisch
    
    CONTEXT D-17: Server-seitige Erkennung
    CONTEXT D-18: charset-normalizer statt chardet
    CONTEXT D-20: Windows-1252 Fallback für deutsche CSVs
    
    Args:
        file_path: Pfad zur CSV-Datei
        
    Returns:
        EncodingResult mit detected_encoding und confidence
    """
    # Read raw bytes
    with open(file_path, 'rb') as f:
        raw_data = f.read()
    
    # Detect encoding with charset-normalizer
    results = from_bytes(raw_data)
    best_match = results.best()
    
    if best_match and best_match.encoding:
        confidence = float(best_match.encoding_confidence)
        detected = best_match.encoding
        
        # RESEARCH: High confidence threshold for German text
        if confidence > 0.7:
            return EncodingResult(
                detected_encoding=detected,
                confidence=confidence,
                is_fallback=False,
                needs_confirmation=False
            )
        else:
            # Low confidence - needs user confirmation (CONTEXT D-19)
            return EncodingResult(
                detected_encoding=detected,
                confidence=confidence,
                is_fallback=False,
                needs_confirmation=True
            )
    
    # Detection failed - fallback to Windows-1252 (CONTEXT D-20)
    return EncodingResult(
        detected_encoding="windows-1252",
        confidence=0.0,
        is_fallback=True,
        needs_confirmation=True  # Always confirm fallback
    )

def convert_to_utf8(
    input_path: Path,
    output_path: Path,
    source_encoding: str
) -> tuple[bool, Optional[str]]:
    """
    Konvertiert CSV von source_encoding nach UTF-8
    
    Args:
        input_path: Original CSV
        output_path: UTF-8 konvertierte Datei
        source_encoding: Quell-Encoding (z.B. "windows-1252")
        
    Returns:
        (success: bool, error_message: Optional[str])
    """
    try:
        # Read with source encoding
        with open(input_path, 'r', encoding=source_encoding, errors='replace') as f:
            content = f.read()
        
        # Write as UTF-8
        with open(output_path, 'w', encoding='utf-8') as f:
            f.write(content)
        
        # Validate umlauts (RESEARCH: check for common German characters)
        if validate_german_umlauts(output_path):
            return True, None
        else:
            return False, "Conversion produced mojibake - umlauts corrupted"
            
    except UnicodeDecodeError as e:
        return False, f"Failed to decode with {source_encoding}: {str(e)}"
    except Exception as e:
        return False, f"Conversion error: {str(e)}"

def validate_german_umlauts(file_path: Path, sample_lines: int = 10) -> bool:
    """
    Prüft ob deutsche Umlaute korrekt sind (kein Mojibake)
    
    Args:
        file_path: UTF-8 Datei zum Prüfen
        sample_lines: Anzahl Zeilen zum Samplen
        
    Returns:
        True wenn Umlaute korrekt, False bei Mojibake
    """
    try:
        with open(file_path, 'r', encoding='utf-8') as f:
            sample = ''.join([f.readline() for _ in range(sample_lines)])
        
        # Check for mojibake patterns that indicate wrong encoding
        mojibake_patterns = [
            'Ã¤',  # ä encoded as Windows-1252 read as UTF-8
            'Ã¶',  # ö
            'Ã¼',  # ü
            'ÃŸ',  # ß
            'Ã\x84',  # Ä
            'Ã\x96',  # Ö
            'Ã\x9c',  # Ü
        ]
        
        for pattern in mojibake_patterns:
            if pattern in sample:
                return False  # Mojibake detected
        
        return True  # Looks good
        
    except Exception:
        return False  # If we can't read, assume bad encoding
```

Update backend/requirements.txt mit neuen Dependencies.
  </action>
  
  <acceptance_criteria>
- `backend/app/services/encoding.py` exists and contains `def detect_encoding(file_path: Path) -> EncodingResult`
- `backend/app/services/encoding.py` contains `def convert_to_utf8` function
- `backend/app/services/encoding.py` contains `def validate_german_umlauts` function that checks for mojibake patterns (Ã¤, Ã¶, Ã¼, ÃŸ)
- `backend/app/services/encoding.py` uses `from_bytes` from `charset_normalizer` (not chardet)
- `detect_encoding` returns Windows-1252 as fallback when confidence < 0.7 or detection fails (implements CONTEXT D-20)
- `backend/requirements.txt` contains `charset-normalizer==3.3.2` and `polars==0.20.10`
  </acceptance_criteria>
  
  <verify>
```bash
cd backend

# Test encoding detection on Windows-1252 CSV
python3 << 'EOF'
from pathlib import Path
from app.services.encoding import detect_encoding, convert_to_utf8
import tempfile

# Create test CSV with German umlauts in Windows-1252
test_content = "Artikelnummer,Bezeichnung\nD80950,Müllbehälter"
with tempfile.NamedTemporaryFile(mode='w', encoding='windows-1252', suffix='.csv', delete=False) as f:
    f.write(test_content)
    test_path = Path(f.name)

try:
    result = detect_encoding(test_path)
    print(f"Detected: {result.detected_encoding}, Confidence: {result.confidence}")
    assert result.detected_encoding in ["windows-1252", "cp1252", "iso-8859-1"], f"Wrong encoding: {result.detected_encoding}"
    
    # Test conversion
    output_path = test_path.with_suffix('.utf8.csv')
    success, error = convert_to_utf8(test_path, output_path, result.detected_encoding)
    assert success, f"Conversion failed: {error}"
    
    # Verify umlauts
    with open(output_path, 'r', encoding='utf-8') as f:
        content = f.read()
    assert 'Müllbehälter' in content, "Umlauts corrupted"
    print("✓ Encoding detection and conversion working")
    
finally:
    test_path.unlink()
    if output_path.exists():
        output_path.unlink()
EOF
```
  </verify>
  
  <done>
- Encoding-Erkennung funktioniert für Windows-1252 und UTF-8 CSVs
- Fallback zu Windows-1252 bei niedriger Confidence
- Konvertierung nach UTF-8 ohne Umlaut-Korruption
- Mojibake-Validierung erkennt falsche Encodings
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: CSV Validation Service mit Warning-System</name>
  <files>backend/app/services/validation.py, backend/app/models/validation.py</files>
  
  <read_first>
- backend/app/services/encoding.py (encoding detection results)
- .planning/phases/01-backend-foundation-data-import/01-CONTEXT.md (D-09 bis D-12: Sofortige Validierung, non-blocking warnings, erweiterte Prüfung, early-exit)
- .planning/phases/01-backend-foundation-data-import/01-RESEARCH.md (Polars validation patterns, line-specific errors)
  </read_first>
  
  <action>
**backend/app/models/validation.py:**
```python
"""Validation error models"""
from pydantic import BaseModel, Field
from enum import Enum
from typing import Optional

class ErrorSeverity(str, Enum):
    """Fehler-Schweregrad"""
    CRITICAL = "critical"  # Blockiert Processing
    WARNING = "warning"    # Kann fortfahren (CONTEXT D-10)
    INFO = "info"          # Nur FYI

class ValidationError(BaseModel):
    """
    Strukturierter Validierungsfehler
    CONTEXT D-14: Zeilenspezifische Fehler wo möglich
    """
    severity: ErrorSeverity
    file: str
    line: Optional[int] = None  # None für file-level errors
    column: Optional[str] = None
    message: str
    suggestion: Optional[str] = None  # Actionable fix (CONTEXT D-16)
    
class ValidationResult(BaseModel):
    """Ergebnis der CSV-Validierung"""
    upload_id: str
    file: str
    status: str  # "valid", "warnings", "errors"
    errors: list[ValidationError] = Field(default_factory=list)
    warnings: list[ValidationError] = Field(default_factory=list)
    stats: dict = Field(default_factory=dict)  # rows, columns, encoding
```

**backend/app/services/validation.py:**
```python
"""
CSV validation service
CONTEXT D-11: Erweiterte Prüfung (Struktur + Inhalt)
CONTEXT D-12: Early-exit Performance-Strategie
"""
from pathlib import Path
import polars as pl
from typing import Optional

from app.models.validation import ValidationError, ValidationResult, ErrorSeverity

def validate_csv_structure(
    csv_path: Path,
    upload_id: str,
    encoding: str = "utf-8"
) -> ValidationResult:
    """
    Validiert CSV-Struktur und Inhalt (CONTEXT D-11)
    
    Phase 1 validation:
    - CSV parsable (basic structure)
    - Encoding correct (Umlaute vorhanden)
    - Has rows and columns
    - Basic duplicate check (Artikelnummer)
    
    Deferred to Phase 2:
    - Column semantics (which column is Artikelnummer)
    - Article number format validation
    - Cross-CSV duplicate detection
    - Data completeness checks
    
    Args:
        csv_path: Path to CSV file (already UTF-8 converted)
        upload_id: Upload session ID
        encoding: File encoding (should be utf-8 after conversion)
        
    Returns:
        ValidationResult with errors/warnings
    """
    errors: list[ValidationError] = []
    warnings: list[ValidationError] = []
    stats = {}
    
    # CONTEXT D-12: Early-exit - stop at first critical error
    try:
        # Try to parse CSV with Polars (lazy evaluation)
        df = pl.scan_csv(
            csv_path,
            encoding=encoding,
            ignore_errors=False,  # Fail on parse errors
            truncate_ragged_lines=False  # Detect malformed rows
        )
        
        #Check 1: Empty file?
        try:
            row_count = df.select(pl.count()).collect().item()
        except Exception as e:
            errors.append(ValidationError(
                severity=ErrorSeverity.CRITICAL,
                file=csv_path.name,
                line=None,
                column=None,
                message=f"Failed to parse CSV: {str(e)}",
                suggestion="Check file format and encoding"
            ))
            return ValidationResult(
                upload_id=upload_id,
                file=csv_path.name,
                status="errors",
                errors=errors,
                stats={}
            )
        
        if row_count == 0:
            errors.append(ValidationError(
                severity=ErrorSeverity.CRITICAL,
                file=csv_path.name,
                line=None,
                column=None,
                message="CSV file is empty",
                suggestion="Upload a file with product data"
            ))
            return ValidationResult(
                upload_id=upload_id,
                file=csv_path.name,
                status="errors",
                errors=errors,
                stats={"rows": 0}
            )
        
        # Check 2: Has columns?
        columns = df.columns
        stats["rows"] = row_count
        stats["columns"] = len(columns)
        
        if len(columns) == 0:
            errors.append(ValidationError(
                severity=ErrorSeverity.CRITICAL,
                file=csv_path.name,
                line=1,
                column=None,
                message="No columns detected in CSV",
                suggestion="Ensure first row contains column headers"
            ))
            return ValidationResult(
                upload_id=upload_id,
                file=csv_path.name,
                status="errors",
                errors=errors,
                stats=stats
            )
        
        # Check 3: Look for Artikelnummer column (extended check - CONTEXT D-11)
        # Note: Column semantic detection is Phase 2, but basic check here
        artikelnummer_cols = [col for col in columns if 'artikelnummer' in col.lower() or 'artikel-nr' in col.lower()]
        
        if not artikelnummer_cols:
            warnings.append(ValidationError(
                severity=ErrorSeverity.WARNING,  # WARNING not CRITICAL (CONTEXT D-10)
                file=csv_path.name,
                line=1,
                column=None,
                message="No 'Artikelnummer' column found",
                suggestion="Phase 2 will detect column semantics - you can proceed"
            ))
        else:
            # Check 4: Duplicate article numbers? (limited sample for performance)
            artikelnummer_col = artikelnummer_cols[0]
            stats["artikelnummer_column"] = artikelnummer_col
            
            # CONTEXT D-12: Early-exit - only check first 500 rows for duplicates
            sample_df = df.select(artikelnummer_col).head(500).collect()
            
            duplicated = sample_df.filter(pl.col(artikelnummer_col).is_duplicated())
            if len(duplicated) > 0:
                first_dup = duplicated.row(0)[0]
                warnings.append(ValidationError(
                    severity=ErrorSeverity.WARNING,
                    file=csv_path.name,
                    line=None,  # Would need full scan to find exact line
                    column=artikelnummer_col,
                    message=f"Duplicate article numbers found (first: {first_dup})",
                    suggestion="Review data in table interface (Phase 6)"
                ))
        
        # Check 5: Validate German umlauts present (encoding check)
        # Sample first 100 rows to look for German characters
        sample_text = df.head(100).collect().to_pandas().to_string()
        has_german_chars = any(char in sample_text for char in ['ä', 'ö', 'ü', 'ß', 'Ä', 'Ö', 'Ü'])
        
        if not has_german_chars:
            warnings.append(ValidationError(
                severity=ErrorSeverity.WARNING,
                file=csv_path.name,
                line=None,
                column=None,
                message="No German umlauts detected - encoding may be incorrect",
                suggestion="Verify product names display correctly in UI"
            ))
        
        # Determine status
        if errors:
            status = "errors"
        elif warnings:
            status = "warnings"
        else:
            status = "valid"
        
        return ValidationResult(
            upload_id=upload_id,
            file=csv_path.name,
            status=status,
            errors=errors,
            warnings=warnings,
            stats=stats
        )
        
    except Exception as e:
        # Catch-all for unexpected errors
        errors.append(ValidationError(
            severity=ErrorSeverity.CRITICAL,
            file=csv_path.name,
            line=None,
            column=None,
            message=f"Validation failed: {str(e)}",
            suggestion="Check file format"
        ))
        return ValidationResult(
            upload_id=upload_id,
            file=csv_path.name,
            status="errors",
            errors=errors,
            stats=stats
        )
```
  </action>
  
  <acceptance_criteria>
- `backend/app/models/validation.py` contains `class ErrorSeverity(str, Enum)` with CRITICAL, WARNING, INFO
- `backend/app/models/validation.py` contains `class ValidationError(BaseModel)` with severity, file, line, column, message, suggestion fields
- `backend/app/models/validation.py` contains `class ValidationResult(BaseModel)` with status field that can be "valid", "warnings", or "errors"
- `backend/app/services/validation.py` contains `def validate_csv_structure` function
- `validate_csv_structure` uses `pl.scan_csv` from polars (not pandas)
- Validation returns WARNING severity for missing Artikelnummer and duplicates (not CRITICAL - implements CONTEXT D-10 non-blocking warnings)
- Validation checks for German umlauts (ä, ö, ü, ß) to verify encoding
- Early-exit: function returns immediately on first CRITICAL error (implements CONTEXT D-12)
  </acceptance_criteria>
  
  <verify>
```bash
cd backend

# Test validation with valid CSV
python3 << 'EOF'
from pathlib import Path
from app.services.validation import validate_csv_structure
import tempfile

# Create valid CSV
valid_csv = tempfile.NamedTemporaryFile(mode='w', encoding='utf-8', suffix='.csv', delete=False)
valid_csv.write("Artikelnummer,Bezeichnung,Preis\n")
valid_csv.write("D80950,Müllbehälter,45.99\n")
valid_csv.write("D80951,Abfalltonne,89.99\n")
valid_csv.close()

try:
    result = validate_csv_structure(Path(valid_csv.name), "test-session")
    assert result.status == "valid", f"Expected valid, got {result.status}"
    assert result.stats["rows"] == 2
    assert len(result.errors) == 0
    print("✓ Valid CSV passes validation")
finally:
    Path(valid_csv.name).unlink()

# Test validation with missing Artikelnummer column
no_artikel_csv = tempfile.NamedTemporaryFile(mode='w', encoding='utf-8', suffix='.csv', delete=False)
no_artikel_csv.write("ProductID,Name,Price\n")
no_artikel_csv.write("12345,Product A,10.00\n")
no_artikel_csv.close()

try:
    result = validate_csv_structure(Path(no_artikel_csv.name), "test-session")
    assert result.status == "warnings", "Missing Artikelnummer should produce warnings"
    assert len(result.warnings) > 0
    assert any('Artikel' in w.message for w in result.warnings)
    print("✓ Missing Artikelnummer produces warning (non-blocking)")
finally:
    Path(no_artikel_csv.name).unlink()

# Test empty CSV
empty_csv = tempfile.NamedTemporaryFile(mode='w', encoding='utf-8', suffix='.csv', delete=False)
empty_csv.write("Artikelnummer,Bezeichnung\n")  # Header only
empty_csv.close()

try:
    result = validate_csv_structure(Path(empty_csv.name), "test-session")
    assert result.status == "errors", "Empty CSV should produce errors"
    assert len(result.errors) > 0
    assert any('empty' in e.message.lower() for e in result.errors)
    print("✓ Empty CSV produces error")
finally:
    Path(empty_csv.name).unlink()
EOF
```
  </verify>
  
  <done>
- Validierung erkennt leere CSVs (CRITICAL error)
- Validierung erkennt fehlende Artikelnummer-Spalte (WARNING - non-blocking)
- Validierung prüft auf deutsche Umlaute
- Warnings blockieren nicht (status="warnings" erlaubt Fortfahren)
- Early-exit bei erstem CRITICAL error
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Integration in Upload API</name>
  <files>backend/app/api/upload.py</files>
  
  <read_first>
- backend/app/api/upload.py (existing upload endpoints from Plan 01)
- backend/app/services/encoding.py (encoding detection)
- backend/app/services/validation.py (validation logic)
- backend/app/models/validation.py (validation response models)
  </read_first>
  
  <action>
Update `backend/app/api/upload.py` to integrate encoding detection and validation:

**backend/app/api/upload.py** (modify existing upload_csv endpoint):
```python
from fastapi import APIRouter, UploadFile, File, HTTPException, BackgroundTasks
from fastapi.responses import JSONResponse
from pathlib import Path
from datetime import datetime
import aiofiles
import os

from app.core.config import settings
from app.models.upload import CSVUploadResponse, ImageUploadResponse, UploadSession
from app.models.validation import ValidationResult
from app.services.encoding import detect_encoding, convert_to_utf8
from app.services.validation import validate_csv_structure

router = APIRouter()

# ... (keep existing helper functions)...

@router.post("/upload/csv", response_model=dict)  # Changed return type
async def upload_csv(
    file: UploadFile = File(...),
):
    """
    Upload CSV with encoding detection and validation
    
    Returns:
        {
            "upload": CSVUploadResponse,
            "encoding": EncodingResult,
            "validation": ValidationResult
        }
    """
    # Validation: CSV extension
    if not file.filename.lower().endswith('.csv'):
        raise HTTPException(
            status_code=400,
            detail=f"Invalid file type. Expected .csv, got {Path(file.filename).suffix}"
        )
    
    # Validation: File size
    if file.size and file.size > settings.max_csv_size_mb * 1024 * 1024:
        raise HTTPException(
            status_code=413,
            detail=f"File too large. Max {settings.max_csv_size_mb}MB allowed."
        )
    
    # Create upload session
    upload_id, upload_dir = create_upload_session()
    
    # Save original file
    original_path = upload_dir / f"{file.filename}.original"
    file_size = 0
    
    async with aiofiles.open(original_path, 'wb') as f:
        while chunk := await file.read(8192):
            file_size += len(chunk)
            await f.write(chunk)
    
    # STEP 1: Detect encoding (NEW)
    encoding_result = detect_encoding(original_path)
    
    # STEP 2: Convert to UTF-8 (NEW)
    utf8_path = upload_dir / file.filename
    success, error = convert_to_utf8(
        original_path,
        utf8_path,
        encoding_result.detected_encoding
    )
    
    if not success:
        raise HTTPException(
            status_code=422,
            detail=f"Encoding conversion failed: {error}"
        )
    
    # STEP 3: Validate CSV structure (NEW)
    validation_result = validate_csv_structure(
        utf8_path,
        upload_id,
        encoding="utf-8"
    )
    
    # Build response
    upload_response = CSVUploadResponse(
        upload_id=upload_id,
        filename=file.filename,
        size_bytes=file_size,
        uploaded_at=datetime.now(),
        path=str(utf8_path.relative_to(settings.upload_dir))
    )
    
    return {
        "upload": upload_response.dict(),
        "encoding": {
            "detected": encoding_result.detected_encoding,
            "confidence": encoding_result.confidence,
            "is_fallback": encoding_result.is_fallback,
            "needs_confirmation": encoding_result.needs_confirmation
        },
        "validation": validation_result.dict()
    }

# ... (keep existing upload_images and get_upload_session endpoints) ...
```

Add new endpoint for encoding confirmation:

**backend/app/api/upload.py** (add new endpoint):
```python
@router.post("/upload/csv/{upload_id}/confirm-encoding")
async def confirm_encoding(
    upload_id: str,
    confirmed_encoding: str
):
    """
    User confirms or overrides detected encoding
    CONTEXT D-19: Benutzer-Bestätigung bei unsicherer Erkennung
    
    Args:
        upload_id: Upload session ID
        confirmed_encoding: User-confirmed encoding (e.g., "windows-1252", "utf-8")
    
    Returns:
        Re-validated CSV with new encoding
    """
    upload_dir = settings.upload_dir / upload_id
    
    if not upload_dir.exists():
        raise HTTPException(status_code=404, detail="Upload session not found")
    
    # Find original CSV
    original_files = list(upload_dir.glob("*.csv.original"))
    if not original_files:
        raise HTTPException(status_code=404, detail="Original CSV not found")
    
    original_path = original_files[0]
    utf8_path = upload_dir / original_path.stem  # Remove .original suffix
    
    # Re-convert with confirmed encoding
    success, error = convert_to_utf8(
        original_path,
        utf8_path,
        confirmed_encoding
    )
    
    if not success:
        raise HTTPException(
            status_code=422,
            detail=f"Conversion with {confirmed_encoding} failed: {error}"
        )
    
    # Re-validate
    validation_result = validate_csv_structure(utf8_path, upload_id)
    
    return {
        "encoding": confirmed_encoding,
        "validation": validation_result.dict()
    }
```
  </action>
  
  <acceptance_criteria>
- `backend/app/api/upload.py` imports `detect_encoding`, `convert_to_utf8` from `app.services.encoding`
- `backend/app/api/upload.py` imports `validate_csv_structure` from `app.services.validation`
- `backend/app/api/upload.py` imports `ValidationResult` from `app.models.validation`
- `upload_csv` endpoint now returns dict with "upload", "encoding", and "validation" keys (not just CSVUploadResponse)
- Upload flow: saves original → detects encoding → converts to UTF-8 → validates structure
- New endpoint `POST /api/upload/csv/{upload_id}/confirm-encoding` exists for user encoding override (implements CONTEXT D-19)
- Original CSV saved as `.original` suffix to allow re-conversion with different encoding
  </acceptance_criteria>
  
  <verify>
```bash
# Create test CSV with Windows-1252 encoding
echo "Artikelnummer,Bezeichnung" | iconv -f UTF-8 -t WINDOWS-1252 > /tmp/test-1252.csv
echo "D80950,Müllbehälter" | iconv -f UTF-8 -t WINDOWS-1252 >> /tmp/test-1252.csv

# Start backend
docker-compose up -d backend && sleep 3

# Upload CSV and check encoding detection
RESPONSE=$(curl -X POST -F "file=@/tmp/test-1252.csv" http://localhost:8000/api/upload/csv)
echo "$RESPONSE" | jq '.'

# Verify response structure
echo "$RESPONSE" | jq -e '.upload.upload_id' || echo "Missing upload_id"
echo "$RESPONSE" | jq -e '.encoding.detected' || echo "Missing encoding.detected"
echo "$RESPONSE" | jq -e '.validation.status' || echo "Missing validation.status"

# Check encoding detected correctly
DETECTED=$(echo "$RESPONSE" | jq -r '.encoding.detected')
echo "Detected encoding: $DETECTED"
[[ "$DETECTED" == "windows-1252" || "$DETECTED" == "cp1252" ]] || echo "Wrong encoding detected"

# Verify UTF-8 conversion
UPLOAD_ID=$(echo "$RESPONSE" | jq -r '.upload.upload_id')
UTF8_FILE="uploads/${UPLOAD_ID}/test-1252.csv"
file -b --mime-encoding "$UTF8_FILE" | grep -q "utf-8" || echo "Not converted to UTF-8"

# Check umlauts correct
grep -q "Müllbehälter" "$UTF8_FILE" || echo "Umlauts corrupted"

docker-compose down
rm /tmp/test-1252.csv
```
  </verify>
  
  <done>
- CSV upload automatisch detectet encoding
- Original wird zu UTF-8 konvertiert
- Deutsche Umlaute korrekt in konvertierter Datei
- Validation läuft nach Konvertierung
- Benutzer kann encoding confirmation override nutzen (Endpoint vorhanden)
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Tests für Encoding + Validation</name>
  <files>backend/tests/test_encoding.py, backend/tests/test_validation.py</files>
  
 <read_first>
- backend/app/services/encoding.py (functions to test)
- backend/app/services/validation.py (validation to test)
- backend/tests/conftest.py (test fixtures)
  </read_first>
  
  <action>
**backend/tests/test_encoding.py:**
```python
import pytest
from pathlib import Path
import tempfile

from app.services.encoding import (
    detect_encoding,
    convert_to_utf8,
    validate_german_umlauts
)

def test_detect_windows_1252_encoding():
    """Test CONTEXT D-20: Windows-1252 detection für deutsche CSVs"""
    # Create CSV with German umlauts in Windows-1252
    with tempfile.NamedTemporaryFile(
        mode='w',
        encoding='windows-1252',
        suffix='.csv',
        delete=False
    ) as f:
        f.write("Artikelnummer,Bezeichnung\n")
        f.write("D80950,Müllbehälter\n")
        f.write("D80951,Größe 240L\n")
        test_file = Path(f.name)
    
    try:
        result = detect_encoding(test_file)
        # Should detect Windows-1252 or compatible (cp1252, iso-8859-1)
        assert result.detected_encoding.lower() in [
            'windows-1252', 'cp1252', 'iso-8859-1', 'iso88591'
        ]
        # High confidence or fallback
        assert result.confidence > 0.6 or result.is_fallback
    finally:
        test_file.unlink()

def test_detect_utf8_encoding():
    """Test UTF-8 detection"""
    with tempfile.NamedTemporaryFile(
        mode='w',
        encoding='utf-8',
        suffix='.csv',
        delete=False
    ) as f:
        f.write("Artikelnummer,Bezeichnung\n")
        f.write("D80950,Müllbehälter\n")
        test_file = Path(f.name)
    
    try:
        result = detect_encoding(test_file)
        assert result.detected_encoding.lower() in ['utf-8', 'utf_8', 'ascii']
        assert result.confidence > 0.7
        assert not result.needs_confirmation  # High confidence
    finally:
        test_file.unlink()

def test_convert_windows1252_to_utf8_preserves_umlauts():
    """Test RESEARCH Pitfall #3: Umlaut corruption prevention"""
    # Create Windows-1252 CSV
    input_file = tempfile.NamedTemporaryFile(
        mode='w',
        encoding='windows-1252',
        suffix='.csv',
        delete=False
    )
    input_file.write("Bezeichnung\nMüllbehälter\nGrößer\näöüß\n")
    input_file.close()
    input_path = Path(input_file.name)
    
    output_path = input_path.with_suffix('.utf8.csv')
    
    try:
        success, error = convert_to_utf8(input_path, output_path, 'windows-1252')
        assert success, f"Conversion failed: {error}"
        
        # Verify UTF-8 encoding
        with open(output_path, 'r', encoding='utf-8') as f:
            content = f.read()
        
        # Check umlauts preserved
        assert 'Müllbehälter' in content
        assert 'Größer' in content
        assert 'äöüß' in content
        
        # Check no mojibake
        assert 'Ã¤' not in content  # Would indicate wrong encoding
        assert 'Ã¶' not in content
        assert 'Ã¼' not in content
        
    finally:
        input_path.unlink()
        if output_path.exists():
            output_path.unlink()

def test_validate_german_umlauts_detects_mojibake():
    """Test mojibake detection"""
    # Create file with mojibake (Windows-1252 bytes read as UTF-8)
    mojibake_file = tempfile.NamedTemporaryFile(
        mode='w',
        encoding='utf-8',
        suffix='.csv',
        delete=False
    )
    mojibake_file.write("Bezeichnung\nMÃ¼llbehÃ¤lter\n")  # Mojibake for "Müllbehälter"
    mojibake_file.close()
    mojibake_path = Path(mojibake_file.name)
    
    try:
        is_valid = validate_german_umlauts(mojibake_path)
        assert not is_valid, "Should detect mojibake"
    finally:
        mojibake_path.unlink()

def test_validate_german_umlauts_accepts_correct_encoding():
    """Test correct umlauts pass validation"""
    correct_file = tempfile.NamedTemporaryFile(
        mode='w',
        encoding='utf-8',
        suffix='.csv',
        delete=False
    )
    correct_file.write("Bezeichnung\nMüllbehälter\näöüßÄÖÜ\n")
    correct_file.close()
    correct_path = Path(correct_file.name)
    
    try:
        is_valid = validate_german_umlauts(correct_path)
        assert is_valid, "Should accept correct umlauts"
    finally:
        correct_path.unlink()
```

**backend/tests/test_validation.py:**
```python
import pytest
from pathlib import Path
import tempfile

from app.services.validation import validate_csv_structure
from app.models.validation import ErrorSeverity

def test_validation_passes_valid_csv():
    """Test validation on valid German CSV"""
    csv_file = tempfile.NamedTemporaryFile(
        mode='w',
        encoding='utf-8',
        suffix='.csv',
        delete=False
    )
    csv_file.write("Artikelnummer,Bezeichnung1,Preis\n")
    csv_file.write("D80950,Müllbehälter 120L,45.99\n")
    csv_file.write("D80951,Abfalltonne 240L,89.99\n")
    csv_file.close()
    csv_path = Path(csv_file.name)
    
    try:
        result = validate_csv_structure(csv_path, "test-session")
        assert result.status == "valid"
        assert len(result.errors) == 0
        assert result.stats["rows"] == 2
        assert "Artikelnummer" in result.stats["artikelnummer_column"]
    finally:
        csv_path.unlink()

def test_validation_warnings_for_missing_artikelnummer():
    """Test CONTEXT D-10: Non-blocking warnings"""
    csv_file = tempfile.NamedTemporaryFile(
        mode='w',
        encoding='utf-8',
        suffix='.csv',
        delete=False
    )
    csv_file.write("ProductID,Name,Price\n")
    csv_file.write("12345,Product A,10.00\n")
    csv_file.close()
    csv_path = Path(csv_file.name)
    
    try:
        result = validate_csv_structure(csv_path, "test-session")
        # Should be warnings, not errors (CONTEXT D-10)
        assert result.status == "warnings"
        assert len(result.errors) == 0  # No CRITICAL errors
        assert len(result.warnings) > 0
        # Check warning mentions Artikelnummer
        assert any('artikelnummer' in w.message.lower() for w in result.warnings)
        # Check severity is WARNING not CRITICAL
        assert all(w.severity == ErrorSeverity.WARNING for w in result.warnings)
    finally:
        csv_path.unlink()

def test_validation_early_exit_on_empty_csv():
    """Test CONTEXT D-12: Early-exit bei critical errors"""
    csv_file = tempfile.NamedTemporaryFile(
        mode='w',
        encoding='utf-8',
        suffix='.csv',
        delete=False
    )
    csv_file.write("Artikelnummer,Bezeichnung\n")  # Header only, no data
    csv_file.close()
    csv_path = Path(csv_file.name)
    
    try:
        result = validate_csv_structure(csv_path, "test-session")
        assert result.status == "errors"
        assert len(result.errors) > 0
        # Check it's a CRITICAL error
        assert result.errors[0].severity == ErrorSeverity.CRITICAL
        assert 'empty' in result.errors[0].message.lower()
    finally:
        csv_path.unlink()

def test_validation_detects_duplicate_artikelnummer():
    """Test duplicate detection (WARNING not ERROR)"""
    csv_file = tempfile.NamedTemporaryFile(
        mode='w',
        encoding='utf-8',
        suffix='.csv',
        delete=False
    )
    csv_file.write("Artikelnummer,Bezeichnung,Preis\n")
    csv_file.write("D80950,Product A,10.00\n")
    csv_file.write("D80950,Product A Duplicate,15.00\n")  # Same Artikelnummer
    csv_file.write("D80951,Product B,20.00\n")
    csv_file.close()
    csv_path = Path(csv_file.name)
    
    try:
        result = validate_csv_structure(csv_path, "test-session")
        # Duplicates should be WARNING not ERROR
        assert result.status == "warnings"
        assert any('duplicate' in w.message.lower() for w in result.warnings)
        assert any(w.column == "Artikelnummer" for w in result.warnings)
    finally:
        csv_path.unlink()

def test_validation_warns_about_missing_german_chars():
    """Test German umlaut validation warning"""
    csv_file = tempfile.NamedTemporaryFile(
        mode='w',
        encoding='utf-8',
        suffix='.csv',
        delete=False
    )
    csv_file.write("ProductID,Name,Price\n")
    csv_file.write("12345,Product A,10.00\n")  # No German characters
    csv_file.write("12346,Product B,15.00\n")
    csv_file.close()
    csv_path = Path(csv_file.name)
    
    try:
        result = validate_csv_structure(csv_path, "test-session")
        # Should warn about missing German characters
        assert result.status == "warnings"
        assert any('umlaut' in w.message.lower() or 'encoding' in w.message.lower() 
                   for w in result.warnings)
    finally:
        csv_path.unlink()
```
  </action>
  
  <acceptance_criteria>
- `backend/tests/test_encoding.py` contains test for Windows-1252 detection
- `backend/tests/test_encoding.py` contains test for UTF-8 to Windows-1252 conversion preserving umlauts (ä,ö,ü,ß)
- `backend/tests/test_encoding.py` contains test for mojibake detection (Ã¤, Ã¶, Ã¼)
- `backend/tests/test_validation.py` contains test verifying missing Artikelnummer produces WARNING not CRITICAL (tests CONTEXT D-10)
- `backend/tests/test_validation.py` contains test for empty CSV producing CRITICAL error with early-exit (tests CONTEXT D-12)
- `backend/tests/test_validation.py` contains test for duplicate article numbers producing WARNING
- All tests create temporary files and clean them up in finally blocks
  </acceptance_criteria>
  
  <verify>
```bash
cd backend
pytest tests/test_encoding.py tests/test_validation.py -v
# All tests should pass
```
  </verify>
  
  <done>
- Encoding tests decken Windows-1252, UTF-8, Mojibake-Detection
- Validation tests decken non-blocking warnings, early-exit, Duplikate
- Tests validieren CONTEXT-Entscheidungen (D-10, D-12)
- pytest läuft ohne Fehler
  </done>
</task>

</tasks>

<verification>
**End-to-end Encoding + Validation Flow:**

```bash
# Create test CSV with German umlauts in Windows-1252
printf "Artikelnummer,Bezeichnung1,Preis\n" | iconv -f UTF-8 -t WINDOWS-1252 > /tmp/test_products.csv
printf "D80950,Müllbehälter 120L,45.99\n" | iconv -f UTF-8 -t WINDOWS-1252 >> /tmp/test_products.csv
printf "D80951,Größere Tonne,89.99\n" | iconv -f UTF-8 -t WINDOWS-1252 >> /tmp/test_products.csv

# Upload CSV
docker-compose up -d backend && sleep 3
RESPONSE=$(curl -X POST -F "file=@/tmp/test_products.csv" http://localhost:8000/api/upload/csv 2>/dev/null)

# Check encoding detected
echo "$RESPONSE" | jq '.encoding.detected' | grep -iE 'windows-1252|cp1252'

# Check validation passed or has warnings only
STATUS=$(echo "$RESPONSE" | jq -r '.validation.status')
[[ "$STATUS" == "valid" || "$STATUS" == "warnings" ]] || echo "Unexpected validation status: $STATUS"

# Verify umlauts in converted file
UPLOAD_ID=$(echo "$RESPONSE" | jq -r '.upload.upload_id')
grep -q "Müllbehälter" "uploads/${UPLOAD_ID}/test_products.csv"
grep -q "Größere" "uploads/${UPLOAD_ID}/test_products.csv"

# Verify no mojibake
! grep -q "Ã¼" "uploads/${UPLOAD_ID}/test_products.csv"
! grep -q "Ã¶" "uploads/${UPLOAD_ID}/test_products.csv"

echo "✓ Encoding detection and validation working end-to-end"

docker-compose down
rm /tmp/test_products.csv
```
</verification>

<success_criteria>
1. System erkennt CSV-Encoding automatisch (Windows-1252, UTF-8, etc.) mit charset-normalizer (REQ IMPORT-02)
2. Deutsche Umlaute (ä,ö,ü,ß) werden korrekt angezeigt nach UTF-8-Konvertierung (REQ IMPORT-02)
3. System validiert CSV-Struktur (parsbar, Spalten vorhanden, Duplikate) (REQ IMPORT-04)
4. Validierungs-Warnungen blockieren Workflow nicht - status="warnings" erlaubt Fortfahren gemäß CONTEXT D-10
5. Benutzer kann Encoding über POST /api/upload/csv/{upload_id}/confirm-encoding überschreiben (CONTEXT D-19)
6. Tests decken Encoding-Detection, Umlaut-Preservation, Mojibake-Detection, Warning-System
</success_criteria>

<output>
Nach Fertigstellung erstelle `.planning/phases/01-backend-foundation-data-import/01-02-SUMMARY.md` mit:
- Encoding-Detection-Strategie und Confidence-Thresholds
- Validierungs-Regeln (was ist CRITICAL vs WARNING)
- Beispiel ValidationResult JSON
- Mojibake-Patterns die detectet werden
- Known limitations (z.B. line numbers bei early-exit)
</output>
